function lsblk --description 'alias lsblk lsblk -o name,kname,mountpoints,fstype,type,label,size,uuid,model,state'
 command lsblk -o name,kname,mountpoints,fstype,type,label,size,uuid,model,state 
end
